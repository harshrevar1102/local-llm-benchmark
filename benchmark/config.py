# Number of benchmark runs for each configuration
NUM_RUNS = 5

# Temperature values to benchmark
TEMPERATURES = [
    0.0,
    0.2,
    0.4,
    0.6,
]

# Models to benchmark
MODELS = [
    "phi3:mini",
    "llama3.2:latest",
    "mistral:latest",
]